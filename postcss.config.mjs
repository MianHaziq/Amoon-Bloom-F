// Tailwind v4 PostCSS plugin. Kept intentionally minimal.
// cache-bust: 2026-08-31 — force a clean rebuild of this config on the build host
// (invalidates any stale/cached compiled version of this file).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
